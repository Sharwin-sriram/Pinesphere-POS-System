import React from "react";

const RestaurantDashboardPage = () => {
  return (
    <div className="p-6">
      <h1 className="text-[length:var(--text-xl)] font-semibold text-[var(--color-text-primary)]">
        Restaurant dashboard
      </h1>
      <p className="mt-2 text-[length:var(--text-sm)] text-[var(--color-text-secondary)]">
        This is a placeholder route. The existing staff dashboard lives under <code>/restaurant-admin</code>.
      </p>
    </div>
  );
};

export default RestaurantDashboardPage;

